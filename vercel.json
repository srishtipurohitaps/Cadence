{
    "version": 2,
    "installCommand": "npm install --ignore-scripts=false",
    "buildCommand": "npm rebuild better-sqlite3 --build-from-source && npm run build",
    "functions": {
        "api/index.ts": {
            "includeFiles": "{pages,static}/**"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}